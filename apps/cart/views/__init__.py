# This file is intentionally empty.
# It marks `apps/cart/views/` as a Python package so that imports like
# `from apps.cart.views.pages import CartDetailView` and
# `from apps.cart.views.api import CartSyncView` work correctly.